
void fay::inst::<%=data.name%>::buildString(mirror::utils::StringBuilder * sb)
{
	sb->add("<%=data.name%>")
	<%data.props.forEach(it=>{%>
		->add(" ")->add(this-><%=it.name%>)
	<%})%>
		->endl();
}
